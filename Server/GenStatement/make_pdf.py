# address = current_app.config["POST_ADDRESS"]
# # lock = threading.Lock()
# t = threading.Thread(target=post.create_mailbox, args=(user.id, address, db.session))
# t.start()


def make_pdf():
    pass